# build output
bin/
obj/
dist/
distcli/

# IDE / editor
.vs/
.idea/
*.user
*.suo
*.swp

# local runtime artefacts — the names the tool writes today. A stale list here is why a machine's
# own diagnostic dump ended up committed at the repo root; the kept sample lives in docs/.
*.log
roch-gpu-diag.txt
roch-gpu-info.txt

# Local validation tools and machine-specific test output
.test-sdk/
test-results/
