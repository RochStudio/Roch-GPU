<Project Sdk="Microsoft.NET.Sdk">
  <!-- Dependency-free test runner (no xunit) so it builds offline. Run: dotnet run -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\GpuTuner.Core\GpuTuner.Core.csproj" />
  </ItemGroup>
</Project>
